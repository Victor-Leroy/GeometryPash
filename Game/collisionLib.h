#ifndef COLLISION
#define COLLISION

#include <SFML/Graphics.hpp>
#include <cmath> // For std::abs and std::fmod
#include <vector>
#include <cstdlib> // For rand() and srand()
#include <ctime>   // For Time()
#include <algorithm> // For std::remove_if
#include<vector>

using namespace std;

class Obstacle{
    double size;
    double xPos;
    double yPos;
public:
    vector get_position(){
        vector<Obstacle> = xPos;
        vector<Obstacle> = yPos;
    }
};

#endif